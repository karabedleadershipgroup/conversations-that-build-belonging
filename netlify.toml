[build]
  publish = "."
  functions = "netlify/functions"

# Friendly URL: /api/chat -> the chat function
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
  force = true
